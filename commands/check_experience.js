import sequelize from '../db_sq';
import Users from '../db_models';

module.exports = {
	name: 'checkexperience',
	description: 'Check a user\'s experience',
	cooldown: 5,
	execute(message) {
		
	},
}